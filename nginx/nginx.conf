server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 暂时注释掉所有 API 和笔记代理
    # location /api/ {
    #     proxy_pass http://geo-api:3001;
    # }
    # location /notes/ {
    #     proxy_pass http://quartz-notes:8080;
    # }
}
